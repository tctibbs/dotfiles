export SAVEHIST=5000                  # Max saved commands
export HISTSIZE=5000                  # Max commands in session history

# Add ~/.local/bin to PATH for user scripts
export PATH="$HOME/.local/bin:$PATH"
